export interface CounterState {
    counter : number // create data type of that initial state
}

export const initialState :CounterState = {
    counter : 0 // declare the variable used as initial state
}